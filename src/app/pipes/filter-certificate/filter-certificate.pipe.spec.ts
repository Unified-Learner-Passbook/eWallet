import { FilterCertificatePipe } from './filter-certificate.pipe';

describe('FilterCertificatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCertificatePipe();
    expect(pipe).toBeTruthy();
  });
});
